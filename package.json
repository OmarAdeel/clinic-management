{
  "name": "clinic-management",
  "version": "1.0.0",
  "description": "Full-stack clinic management system - React + Express + PostgreSQL",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/src/index.js",
    "setup-db": "node server/database/migrate.js --schema && node server/database/migrate.js --seed"
  },
  "keywords": ["clinic", "management", "healthcare"],
  "author": "Omar Adeel",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
